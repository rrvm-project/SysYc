use crate::basicblock::BasicBlock;
use crate::cfg::FuncCfg;
pub struct LivelinessAnalyzer;
impl LivelinessAnalyzer {
    pub fn analyze(cfg: &mut FuncCfg) {
        for i in &cfg.blocks{
            
        }
    }
}